from django.urls import path
from api.api_events import views

app_name = "users"

urlpatterns = [
    # path(
    #       'user/register',
    #       views.api_registration_view,
    #       name='api_users_create'
    # ),
]
